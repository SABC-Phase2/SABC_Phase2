@model SABC_Phase2.Models.OVRS.OVRS_UserProfileViewModel

@{
    ViewData["BannerText"] = "PROFILE";
    ViewData["Title"] = "Profile";
}

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

<div class="breadcrumb-custom">
    Home<span class="breadcrumb-arrow">&#x2794;</span> <span class="breadcrumb-active">Profile</span>
</div>

<div class="tab-container">
    <div class="profile-page container-fluid">
        <div class="row">
            <div class="col-md-12">
                @using (Html.BeginForm("OVRS_Profiles", "OVRS_User", FormMethod.Post, new { enctype = "multipart/form-data" }))
                {
                    @Html.AntiForgeryToken()

                    <!-- First & Last Name side by side -->
                    <div class="row">
                        <div class="col-md-6">
                            <div class="form-group">
                                @Html.LabelFor(m => m.FirstName, htmlAttributes: new { @class = "control-label" })
                                @Html.TextBoxFor(m => m.FirstName, new { @class = "form-control", placeholder = "Enter your first name" })
                                @Html.ValidationMessageFor(m => m.FirstName, "", new { @class = "text-danger" })
                            </div>
                        </div>

                        <div class="col-md-6">
                            <div class="form-group">
                                @Html.LabelFor(m => m.LastName, htmlAttributes: new { @class = "control-label" })
                                @Html.TextBoxFor(m => m.LastName, new { @class = "form-control", placeholder = "Enter your last name" })
                                @Html.ValidationMessageFor(m => m.LastName, "", new { @class = "text-danger" })
                            </div>
                        </div>
                    </div>

                    <div class="form-group">
                        @Html.LabelFor(m => m.CompanyName)
                        @Html.TextBoxFor(m => m.CompanyName, new { @class = "form-control", placeholder = "Enter your company name" })
                    </div>

                    <hr />

                    <!-- Phone number with editable country code and flag -->
                    <div class="form-group mt-3">
                        @Html.LabelFor(m => m.PhoneNumber)
                        <div class="input-group phone-flex-row">
                            <select id="CountryCode" name="CountryCode" class="form-select country-select">
                                @foreach (var cc in Model.CountryCodes)
                                {
                                    <option value="@cc.DialingCode"
                                            data-flag="@cc.FlagUrl"
                                            selected="@(cc.DialingCode == Model.CountryCode ? "selected" : null)">
                                        @cc.CountryName (@cc.DialingCode)
                                    </option>
                                }
                            </select>
                            @Html.TextBoxFor(m => m.PhoneNumber, new { @class = "form-control phone-input", placeholder = "Enter your phone number" })
                            <span class="input-group-text flag-box">
                                <img id="CountryFlag" src="" alt="Country flag" />
                            </span>
                        </div>
                        @Html.ValidationMessageFor(m => m.PhoneNumber, "", new { @class = "text-danger" })
                    </div>

                    <hr />

                    @* <!-- Email -->
                    <div class="form-group d-flex justify-content-between align-items-center">
                        <div>
                            @Html.LabelFor(m => m.Email)
                            <p id="emailStatic" class="form-static">@Model.Email</p>
                            @Html.TextBoxFor(m => m.Email, new { @class = "form-control d-none", id = "emailInput", type = "email" })
                            @Html.ValidationMessageFor(m => m.Email, "", new { @class = "text-danger" })
                        </div>
                        <a href="javascript:void(0)" class="change-link" onclick="toggleEdit('email')">Change</a>
                    </div> *@
                      <!-- Email (NEW FLOW) -->
                    <div class="form-group d-flex justify-content-between align-items-center">
                        <div>
                            @Html.LabelFor(m => m.Email)
                            <p id="emailStatic" class="form-static mb-0">@Model.Email</p>
                        </div>
                        <a href="javascript:void(0)" class="change-link" id="changeEmailBtn">Change</a>
                    </div>
                    @Html.HiddenFor(m => m.Email) <!-- keep model binding for current email -->


<hr />

<!-- Password -->
<div class="form-group">
    <div class="d-flex justify-content-between align-items-center mb-2">
        <label class="mb-0">Password</label>
        <a href="javascript:void(0)" class="change-link" id="changePasswordBtn">Change</a>
    </div>
    <div id="passwordFields" class="d-none">
        <div class="mb-2">
            @* @Html.LabelFor(m => m.CurrentPassword, "Current Password", new { @class = "form-label" }) *@
            @Html.PasswordFor(m => m.CurrentPassword, new { @class = "form-control", placeholder = "Enter current password", autocomplete = "current-password" })
            @Html.ValidationMessageFor(m => m.CurrentPassword, "", new { @class = "text-danger" })
        </div>
        <div class="mb-2">
            @* @Html.LabelFor(m => m.NewPassword, "New Password", new { @class = "form-label" }) *@
            @Html.PasswordFor(m => m.NewPassword, new { @class = "form-control", placeholder = "Enter new password", autocomplete = "new-password" })
            @Html.ValidationMessageFor(m => m.NewPassword, "", new { @class = "text-danger" })
        </div>
        <div class="mb-2">
            @* @Html.LabelFor(m => m.ConfirmPassword, "Confirm Password", new { @class = "form-label" }) *@
            @Html.PasswordFor(m => m.ConfirmPassword, new { @class = "form-control", placeholder = "Confirm new password", autocomplete = "new-password" })
            @Html.ValidationMessageFor(m => m.ConfirmPassword, "", new { @class = "text-danger" })
        </div>
    </div>
</div>
<hr />

                    <div class="form-group">
                        <label>Delete account</label>
                        <p class="text-muted small">Please note that all of the information will be permanently deleted</p>
                        <button type="button" class="btn btn-danger">Delete account</button>
                    </div>

                    <div class="form-group text-end mt-4">
                        <button type="submit" class="btn btn-success">Save</button>
                    </div>
                }
            </div>
        </div>
    </div>
</div>

<!-- ...other code unchanged... -->
<!-- Email Change Modal -->
<div class="modal fade" id="changeEmailModal" tabindex="-1" aria-labelledby="changeEmailModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <form id="changeEmailForm" autocomplete="off" onsubmit="return false;">
                <div class="modal-header">
                    <h5 class="modal-title" id="changeEmailModalLabel">Change Email Address</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <div class="mb-3">
                        <div class="input-group">
                            <input type="email" class="form-control" id="newEmailInput" name="newEmail" placeholder="Enter new email address" required />
                            <button type="button" class="btn btn-success" id="sendOtpBtn" style="min-width:130px;">Send OTP</button>
                        </div>
                        <div class="invalid-feedback" id="newEmailError" style="display:block"></div>
                    </div>
                    <div id="otpSection" class="mb-3 d-none">
                        <label>Enter OTP Code</label>
                        <div class="d-flex justify-content-between gap-2 mb-2">
                            <input type="text" class="form-control text-center otp-input" maxlength="1" />
                            <input type="text" class="form-control text-center otp-input" maxlength="1" />
                            <input type="text" class="form-control text-center otp-input" maxlength="1" />
                            <input type="text" class="form-control text-center otp-input" maxlength="1" />
                            <input type="text" class="form-control text-center otp-input" maxlength="1" />
                            <input type="text" class="form-control text-center otp-input" maxlength="1" />
                        </div>
                        <button type="button" class="btn btn-primary w-100" id="verifyOtpBtn">Verify OTP</button>
                        <div class="invalid-feedback" id="otpError" style="display:block"></div>
                        <div class="valid-feedback" id="otpSuccess" style="display:block"></div>
                    </div>
                </div>
                <div class="modal-footer">
                    @* <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button> *@
                </div>
            </form>
        </div>
    </div>
</div>


<script>
document.addEventListener('DOMContentLoaded', function () {
    // ---- Display flag next to selected country code ----
    var select = document.getElementById('CountryCode');
    var img = document.getElementById('CountryFlag');
    function updateFlag() {
        var opt = select.options[select.selectedIndex];
        var url = opt.getAttribute('data-flag');
        if (url) {
            img.src = url;
            img.style.display = '';
        } else {
            img.style.display = 'none';
        }
    }
    updateFlag();
    select.addEventListener('change', updateFlag);
   // ---- EMAIL CHANGE FLOW ----
    var changeEmailBtn = document.getElementById('changeEmailBtn');
    var sendOtpBtn = document.getElementById('sendOtpBtn');
    var verifyOtpBtn = document.getElementById('verifyOtpBtn');
    var otpSection = document.getElementById('otpSection');
    var newEmailInput = document.getElementById('newEmailInput');

    // Add spinner inside buttons
    if (sendOtpBtn) {
        sendOtpBtn.innerHTML = '<span class="spinner-border spinner-border-sm d-none" role="status" id="sendOtpSpinner"></span> <span class="btn-text">Send OTP</span>';
    }
    if (verifyOtpBtn) {
        verifyOtpBtn.innerHTML = '<span class="spinner-border spinner-border-sm d-none" role="status" id="verifyOtpSpinner"></span> <span class="btn-text">Verify OTP</span>';
    }

    var sendOtpSpinner = document.getElementById('sendOtpSpinner');
    var verifyOtpSpinner = document.getElementById('verifyOtpSpinner');
    var newEmailError = document.getElementById('newEmailError');

    if (changeEmailBtn) {
        changeEmailBtn.addEventListener('click', function () {
            // Clear inputs/errors when opening modal
            newEmailInput.value = '';
            newEmailInput.classList.remove('is-invalid', 'is-valid');
            document.querySelectorAll('.otp-input').forEach(i => i.value = '');
            document.getElementById('otpError').textContent = '';
            document.getElementById('otpSuccess').textContent = '';
            newEmailError.textContent = '';
            otpSection.classList.add('d-none');

            newEmailInput.removeAttribute('readonly');
            sendOtpBtn.disabled = false;

            var modal = new bootstrap.Modal(document.getElementById('changeEmailModal'));
            modal.show();
        });
    }

    // Send OTP with visual feedback
    if (sendOtpBtn) {
        sendOtpBtn.addEventListener('click', async function () {
            var newEmail = newEmailInput.value.trim();
            newEmailInput.classList.remove('is-invalid', 'is-valid');
            newEmailError.textContent = "";

            if (!newEmail) {
                newEmailError.textContent = "Please enter email.";
                newEmailInput.classList.add('is-invalid');
                return;
            }

            sendOtpSpinner.classList.remove('d-none');
            sendOtpBtn.disabled = true;

            try {
                let res = await fetch('/OVRS_User/SendEmailOtp', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(newEmail)
                });
                let data = await res.json();
                if (data.success) {
                    otpSection.classList.remove('d-none');
                    newEmailError.textContent = '';
                    newEmailInput.classList.remove('is-invalid');
                    newEmailInput.classList.add('is-valid');
                    document.querySelector('.otp-input').focus();
                    newEmailInput.setAttribute('readonly', true);
                    sendOtpBtn.querySelector('.btn-text').textContent = 'OTP Sent';
                } else {
                    newEmailError.textContent = data.message;
                    newEmailInput.value = '';
                    newEmailInput.classList.remove('is-valid');
                    newEmailInput.classList.add('is-invalid');
                    newEmailInput.focus();
                    sendOtpBtn.disabled = false;
                    sendOtpBtn.querySelector('.btn-text').textContent = 'Send OTP';
                    otpSection.classList.add('d-none');
                }
            } catch (err) {
                newEmailError.textContent = "Error sending OTP. Please try again.";
                newEmailInput.value = '';
                newEmailInput.classList.remove('is-valid');
                newEmailInput.classList.add('is-invalid');
                newEmailInput.focus();
                sendOtpBtn.disabled = false;
                sendOtpBtn.querySelector('.btn-text').textContent = 'Send OTP';
                otpSection.classList.add('d-none');
            } finally {
                sendOtpSpinner.classList.add('d-none');
            }
        });
    }
    // ---- OTP input behavior ----
    const otpInputs = document.querySelectorAll('.otp-input');
    otpInputs.forEach((input, index) => {
        input.addEventListener('input', () => {
            if (input.value.length === 1 && index < otpInputs.length - 1) {
                otpInputs[index + 1].focus();
            }
        });
        input.addEventListener('keydown', (e) => {
            if (e.key === 'Backspace' && !input.value && index > 0) {
                otpInputs[index - 1].focus();
            }
        });
    });

    // Verify OTP
    if (verifyOtpBtn) {
        verifyOtpBtn.addEventListener('click', async function () {
            let otpCode = Array.from(otpInputs)
                                .map(i => i.value)
                                .join('');

            if (!otpCode || otpCode.length < 6) {
                document.getElementById('otpError').textContent = "Please enter full 6-digit OTP.";
                return;
            }

            // Show spinner and disable button
            verifyOtpSpinner.classList.remove('d-none');
            verifyOtpBtn.disabled = true;

            try {
                let res = await fetch('/OVRS_User/VerifyEmailOtp', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ otp: otpCode })
                });
                let data = await res.json();
                if (data.success) {
                    document.getElementById('otpError').textContent = '';
                    document.getElementById('otpSuccess').textContent = data.message;
                    document.getElementById('emailStatic').textContent = newEmailInput.value;

                    setTimeout(() => {
                        var modal = bootstrap.Modal.getInstance(document.getElementById('changeEmailModal'));
                        modal.hide();
                    }, 1500);
                } else {
                    document.getElementById('otpError').textContent = data.message;
                    verifyOtpBtn.disabled = false; // re-enable if failed
                }
            } catch (err) {
                document.getElementById('otpError').textContent = "Error verifying OTP.";
                verifyOtpBtn.disabled = false; // re-enable if failed
            } finally {
                verifyOtpSpinner.classList.add('d-none'); // hide spinner
            }
        });
    }
});

// ---- Password toggle remains unchanged ----
function toggleEdit(field) {
    if (field === 'email') {
        document.getElementById("emailStatic").classList.toggle("d-none");
        document.getElementById("emailInput").classList.toggle("d-none");
    }
}

    // Password change toggle
    var changePasswordBtn = document.getElementById('changePasswordBtn');
    var passwordFields = document.getElementById('passwordFields');
    if (changePasswordBtn) {
        changePasswordBtn.addEventListener('click', function () {
            passwordFields.classList.toggle('d-none');
        });
    }
</script>



<style>

.spinner-border {
    width: 1rem;
    height: 1rem;
    vertical-align: middle;
    margin-right: 6px;
}


    #changeEmailModal .input-group .form-control,
#changeEmailModal .input-group .btn {
    height: 44px;
    box-sizing: border-box;
}

    /* Phone input row with country code and flag */
    .phone-flex-row {
        display: flex;
        align-items: stretch;
        width: 100%;
    }

    .country-select {
        min-width: 150px;
        max-width: 180px;
        border-radius: 8px 0 0 8px !important;
        margin-right: -1px;
        height: 44px;
        box-sizing: border-box;
        /* Match Bootstrap .form-control height if customized elsewhere */
    }

    .phone-input {
        flex: 1 1 auto;
        min-width: 0;
        border-radius: 0 !important;
        margin-right: -1px;
        height: 44px;
        box-sizing: border-box;
    }

    .flag-box {
        width: 60px;
        min-width: 60px;
        max-width: 80px;
        height: 44px;
        box-sizing: border-box;
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 0 8px 8px 0 !important;
        background: #fff;
        border-left: 1px solid #ced4da;
        padding: 0;
    }

        .flag-box img#CountryFlag {
            width: 32px;
            height: 20px;
            object-fit: cover;
            border-radius: 3px;
            display: block;
            margin: auto;
            box-shadow: 0 0 2px #9992;
        }
    /* Remove extra spacing from .input-group-text if using Bootstrap */
    .input-group .flag-box {
        padding: 0 !important;
    }

    /* General form styling */
    .profile-page {
        padding: 20px;
    }

    .form-group label {
        font-weight: 600;
        margin-bottom: 6px;
    }

    .form-group {
        margin-bottom: 14px;
    }

    .form-control, .form-select {
        border-radius: 8px;
        padding: 10px 12px;
        height: 44px;
        box-sizing: border-box;
    }

    .form-static {
        padding: 5px 0;
        color: #333;
    }

    .btn-success {
        background-color: #1BA548;
        border-color: #1BA548;
        padding: 10px 25px;
        font-weight: bold;
        border-radius: 8px;
    }

    .btn-danger {
        background-color: #e63946;
        border-color: #e63946;
        border-radius: 8px;
    }

    hr {
        margin: 24px 0;
        border: 0;
        border-top: 3px solid #1BA548;
    }

    .change-link {
        color: #1BA548;
        font-weight: 700;
        text-decoration: none;
    }

        .change-link:hover {
            text-decoration: underline;
        }

    .tab-container {
        background: #fff;
        border-radius: 0 0 16px 16px;
        box-shadow: 0 4px 32px rgba(0,0,0,0.08), 0 1.5px 4px rgba(0,0,0,0.04);
        padding: 52px 32px 24px 32px;
        max-width: 1100px;
        margin: 0 auto 80px auto;
        margin-top: 0;
        position: relative;
    }

    .breadcrumb-custom {
        color: #888;
        font-size: 1.05rem;
        font-weight: 500;
        margin-bottom: 18px;
        margin-top: 32px;
        display: flex;
        align-items: center;
        gap: 0.2em;
    }

        .breadcrumb-custom .breadcrumb-arrow {
            margin: 0 0.5em;
            font-size: 1.25em;
            color: #bbb;
            font-weight: 400;
            user-select: none;
        }

        .breadcrumb-custom .breadcrumb-active {
            color: #1a7f37;
            font-weight: 600;
        }
</style>

